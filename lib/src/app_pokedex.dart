import 'package:flutter/material.dart';

class AppPokedex extends StatelessWidget {
  const AppPokedex({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
